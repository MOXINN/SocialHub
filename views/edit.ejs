<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <title>Edit Post</title>
  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-[#18325a] to-[#253b63] flex items-center justify-center">

  <!-- Edit Post Modal Container -->
  <form action="/edit/<%= post._id %>" method="POST" class="bg-[#243a63] bg-opacity-95 rounded-2xl shadow-2xl p-8 w-[95vw] max-w-md flex flex-col gap-6">
    <!-- Username (customize as needed) -->
    <div class="flex items-center gap-2 mb-2">
      <h3><span class="text-white text-lg">
       <%= user.username %>
      </span></h3>
    </div>

    <label class="block text-gray-200 mb-1 text-base" for="postText">Edit your post</label>
    <textarea
      id="postText"
      name="content"
      required
      class="w-full min-h-[120px] max-h-[250px] rounded-xl bg-[#1d2b47] border border-[#30529e40] text-gray-100 px-4 py-3 text-base mb-2 focus:ring-2 focus:ring-sky-400 focus:outline-none transition"
      placeholder="Type your updated post here..."><%= post.content %></textarea>

    <!-- Button Row -->
    <div class="flex gap-4 mt-2">
      <!-- Upgrade Button -->
      <button
        type="submit"
        name="action"
        value="update"
        class="w-full rounded-lg bg-gradient-to-r from-sky-400 to-blue-700 font-semibold text-white py-2 shadow hover:from-blue-600 hover:to-sky-400 transition"
      >
        Upgrade Post
      </button>
      <!-- Delete Button -->
      <button
        type="submit"
        name="action"
        value="delete"
        onclick="return confirm('Are you sure you want to delete this post?');"
        class="w-full rounded-lg bg-gradient-to-r from-rose-500 to-red-700 font-semibold text-white py-2 shadow hover:from-red-700 hover:to-rose-500 transition"
      >
        Delete Post
      </button>
    </div>
  </form>

</body>
</html>
